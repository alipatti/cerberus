// use actix_web::{get, web, App, HttpServer, Responder};

/// Endpoint to test whether the server is up.
// #[get("/healthcheck")]
// async fn test(json: web::Json<ModeratorRequest>) -> impl Responder {
//     "Hello world"
// }

// #[actix_web::main]
fn main() {

    println!("Hello moderator!")

    // HttpServer::new(|| App::new().service(encrypt).service(sign))
    //     .bind(("127.0.0.1", 8))?
    //     .run()
    //     .await
}
